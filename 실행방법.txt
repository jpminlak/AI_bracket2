# 1) 클론 & 가상환경
git clone <repo-url> C:\aiproject
cd C:\aiproject
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt

# 2) 모델(최초 1번만)
ollama pull qwen2.5:3b-instruct

# 3) LLM API(FastAPI) 실행
python .\MealMind\app.py    # 기본: http://127.0.0.1:8001

# 4) 웹(UI) 실행 (새 터미널)
cd <스프링_프로젝트_루트>   # gradlew 있는 폴더
.\gradlew bootRun

# 5) 브라우저
# API 확인:  http://127.0.0.1:8001/ping
# 페이지:    http://localhost:8080/diet